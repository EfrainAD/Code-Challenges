const p = str => console.log(str)
// let n = Array.from(String(12145), Number);
// const findMissingLetter = (array) => {
function runTheFunc(str, num) {
  if (str != '') 
    return parseInt
    (eval(`${num} ${str}`))
  return num
}
function zero(str='') {return runTheFunc(str, 0)}
function one(str='') { return runTheFunc(str, 1)}
function two(str='') {return runTheFunc(str, 2)}
function three(str= '') {return runTheFunc(str, 3)}
function four(str='') {return runTheFunc(str, 4)}
function five(str='') {return runTheFunc(str, 5)}
function six(str='') {return runTheFunc(str, 6)}
function seven(str='') {return runTheFunc(str, 7)}
function eight(str='') {return runTheFunc(str, 8)}
function nine(str='') {return runTheFunc(str, 9)}

function plus(str) {return ` + ${str}`}
function minus(str) {return ` - ${str}`}
function times(str) {return ` * ${str}`}
function dividedBy(str) {return ` / ${str}`}

p(six(dividedBy(eight())))
  // p(one(plus(one())))
  // p(zero(plus(zero())))
  // p(two(plus(two())))
  // p(two(plus(one())))
  // p(nine(dividedBy(three())))
  // p(nine(times(three())))
  // p(nine(minus(three())))
  // p(b) //false);
  // p(c) //false);
  // p(work(9672)) //,false);
  // p(work(2789)) //,true);
  // p(work(2335)) //,true);